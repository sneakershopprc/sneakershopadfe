class Filter {
  filter (response) {
    return response
    // const status = response.status

    // switch (status) {
    //   case 401:
    //     break
    //     case 403:
    //       break
    //     default:
    //       return response
    // }
  }
}

export default new Filter()
